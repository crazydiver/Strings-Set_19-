var s,s1,max:string;
i:integer;
begin
  readln(s);
  s:=s+' ';
  while pos(' ',s)>0 do
    begin
      s1:=copy(s,1,pos(' ',s)-1);
      delete(s,1,pos(' ',s));
      if length(S1)>length(max)then
        max:=s1;
    end;
  writeln(max);
  writeln(length(max));
end.
